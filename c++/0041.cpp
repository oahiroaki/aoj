#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

void comb(vector<int> nums, int m)
{
	if (m == 0) {
		
	} else {
		
		comb()
	}
}

int main()
{
	vector<int> nums;
	int n;
	for(int i = 0; i < 4; i++)
	{
		cin >> n;
		nums.push_back(n);
	}
	return 0;
}
