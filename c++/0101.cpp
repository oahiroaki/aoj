#include <iostream>
#include <string>

using namespace std;


void replace(string str)
{
	for (int i = 0; i < str.size(); i++)
}

int main()
{
	string before = "Hoshino", after = "Hoshina";
	int num;

	cin >> num;

	for (int i = 0; i < num; i++)
	{
		string temp;
		cin >> temp;
		cout << temp << endl;
	}

	return 0;
}
